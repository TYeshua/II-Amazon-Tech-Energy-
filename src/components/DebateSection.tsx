import { ArrowDown } from 'lucide-react'; import { timelineItems } from '../data/timeline'; import { siteContent } from '../data/siteContent'; import { Reveal } from './Reveal'
export function DebateSection(){return <section className="debate" id="debate"><div className="section-inner narrow"><Reveal><h2>POR QUE DEBATÊ-LA NO<br/>2º AMAZON TECH <span>ENERGY</span>?</h2><p className="intro">{siteContent.debate.intro}</p></Reveal><div className="timeline">{timelineItems.map((item,i)=><Reveal key={item.id} delay={i*110}><article className="timeline-row"><div className="marker">{i<2?<ArrowDown size={15}/>:<i/>}</div><div className="timeline-card"><p><strong>{item.title}:</strong> {item.description}</p></div></article></Reveal>)}</div><Reveal delay={180}><p className="closing">{siteContent.debate.closing}</p></Reveal></div></section>}
